#include <stdio.h>
/**
 *main - start of the program
 *Return: walys 0 to success
 *
 */
int main(void)
{
	/**
	 *we'll use printf because:
	 *You are allowed to use the standard library
	 */
	printf("%s\n", __FILE__);
	return (0);
}
